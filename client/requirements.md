## Packages
framer-motion | Smooth entry animations for text and form elements
react-icons | Specific brand icons like SiMedium if Lucide doesn't have them

## Notes
API endpoint: POST /api/waitlist with { name, email }
Design: Dark mode only, pixel-perfect implementation of the provided spec
Font: Inter (Google Fonts)
